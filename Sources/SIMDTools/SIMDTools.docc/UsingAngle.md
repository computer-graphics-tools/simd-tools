# Using Angle

The `Angle` struct provides a convenient way to represent and manipulate angles in both degrees and radians.

## Creating an Angle

You can create an angle using degrees or radians:

```swift
let angleInDegrees = Angle(degrees: 45)
let angleInRadians = Angle(radians: .pi / 4)

